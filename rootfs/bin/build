#!/bin/sh

set -ef pipefail

# Install common packages useful for anyone exec'ing into a container to troubleshoot 
apt-get update
apt-get install -y \
	bash \
	curl \
	net-tools \
	procps

# Clean up
apt-get clean 
rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/* /usr/share/man /usr/share/doc
